# AEGIS SoftHSM init smoke evidence
# Host: Windows 10 (win32 10.0.26200) + WSL2 Ubuntu 24.04.1 LTS
# Repo tip: f531480 Ship productization wave: pilot pack, SoftHSM path, adaptive mitigation, charter.
# Captured: 2026-07-18T05:02:11Z (UTC)

## Step 1 — apt install (blocked)

Command:
  wsl -e bash -lc "sudo -n true"

Result:
  sudo: a password is required
  SUDO_NEEDS_PASSWORD

Command (not run — would require interactive sudo password):
  sudo apt-get update && sudo apt-get install -y softhsm2 opensc

Pre-check:
  which softhsm2-util → NOT_INSTALLED

Blocker: WSL sudo requires a password on this host; non-interactive install cannot proceed.

## Step 2 — scripts/softhsm_init.sh (graceful no-op)

Command:
  wsl -e bash -lc "cd /mnt/c/Users/odinl/OneDrive/Desktop/ANONYMOUS && bash scripts/softhsm_init.sh"

Exit code: 0

Output:
  SoftHSM2 not found (softhsm2-util missing).

  Install on Debian/Ubuntu WSL:
    sudo apt-get update && sudo apt-get install -y softhsm2 opensc

  Then re-run:
    bash scripts/softhsm_init.sh

  See docs/ops/softhsm_ceremony.md for PKCS#11 module path and AEGIS wiring.

Token init: NOT PERFORMED (softhsm2-util absent)
pkcs11-tool smoke: NOT PERFORMED (opensc not installed)

## Step 3 — PKCS#11 Rust/Python smoke

Skipped intentionally: SoftHSM not installed; no optional in-tree PKCS#11 crate added.
Default workspace build unchanged (no native PKCS#11 deps).

## Step 4 — default cargo test (no SoftHSM)

Windows PowerShell: cargo not on PATH.

WSL (crates/ workspace):
  cd crates && cargo test --workspace

Result (2026-07-18): 6 failures in aegis-node — all KemProtect/keyring
  ("keyring get_password: No matching entry found in secure storage").
  Unrelated to SoftHSM; no PKCS#11 crates in workspace. Other crates passed.

  Failed tests:
    config::tests::kem_first_run_writes_external_file_by_default
    config::tests::kem_load_from_external_file_with_custom_path
    config::tests::kem_load_or_init_is_idempotent
    config::tests::kem_persist_roundtrip_via_protect_layer
    config::tests::link_and_ingress_toml_wire_ingress_rate_limit
    kem_seed_protect::tests::keyring_protect_unprotect_roundtrip_or_fallback

## WSL host metadata

Linux OdinsPC 6.6.87.2-microsoft-standard-WSL2 x86_64 GNU/Linux
PRETTY_NAME="Ubuntu 24.04.1 LTS"

## Unblock for real token smoke

On this machine, run interactively in WSL:

  sudo apt-get update && sudo apt-get install -y softhsm2 opensc
  cd /mnt/c/Users/odinl/OneDrive/Desktop/ANONYMOUS
  bash scripts/softhsm_init.sh | tee -a sim/softhsm_init_evidence.txt

Expected after install: token label aegis-ceremony on slot 0, pkcs11-tool --list-slots OK.

---

# AEGIS SoftHSM no-sudo build attempt (tip 9ce640f)
# Captured: 2026-07-18T05:13:05Z (UTC)

## Step 1 — WSL build-deps pre-check (no sudo)

Host: WSL2 Ubuntu 24.04.1 LTS, user odin (sudo group, password required)

| Dependency | Status |
|------------|--------|
| gcc (13.3.0) | OK — /usr/bin/gcc |
| make (4.3) | OK — /usr/bin/make |
| openssl (3.0.13) | OK — /usr/bin/openssl |
| libssl-dev headers (/usr/include/openssl/ssl.h) | OK |
| pkg-config openssl | OK — 3.0.13 |
| autoconf | OK — /usr/bin/autoconf |
| automake | OK — /usr/bin/automake |
| uuid-dev (/usr/include/uuid/uuid.h) | **MISSING** |
| libtool | **MISSING** |

Exact packages needed (one sudo password prompt):
  sudo apt-get update && sudo apt-get install -y uuid-dev libtool

Optional full build chain if not already present:
  sudo apt-get install -y build-essential libssl-dev autoconf automake

## Step 2 — SoftHSM2 source build to ~/.local

Result: **NOT RUN** — blocked by missing uuid-dev and libtool.
Would run after deps installed:
  mkdir -p ~/src && cd ~/src
  curl -LO https://github.com/opendnssec/SoftHSMv2/archive/refs/tags/2.6.1.tar.gz
  tar xzf 2.6.1.tar.gz && cd SoftHSMv2-2.6.1
  ./autogen.sh && ./configure --prefix=$HOME/.local --disable-gost
  make -j$(nproc) && make install

## Step 3 — scripts/softhsm_init.sh (graceful no-op)

Command:
  wsl -u odin bash -c "cd /mnt/c/Users/odinl/OneDrive/Desktop/ANONYMOUS && bash scripts/softhsm_init.sh"

Exit code: 0

Output:
  SoftHSM2 not found (softhsm2-util missing).
  (install hints for system packages and ~/.local build path)

Token init: NOT PERFORMED
pkcs11-tool smoke: NOT PERFORMED
~/.local/bin/softhsm2-util: NOT PRESENT

## Step 4 — repo updates (this tip)

- docs/ops/softhsm_ceremony.md — added Option B (~/.local prefix paths)
- scripts/softhsm_init.sh — prefers $HOME/.local/bin/softhsm2-util and module path

## Remaining blocker

Non-interactive sudo unavailable. Install uuid-dev + libtool (or softhsm2 opensc)
with one interactive password, then re-run init script.
